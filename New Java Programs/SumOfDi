
		
		while (num>0)
		{
			int rem = num%10;
			sum+= rem;
			num/=10;

		}
		System.out.println(sum);
		System.out.println(dup);

		while (dup>0)
		{
			int rem2 = dup%10;
			if (dup%2==0)
			{
              
			  sum2+=rem2;
			  
			}
			dup/=10;
		}
           System.out.println("Sum of even digits: " + sum2);
		
	}
}
